# ImageryService

```
google.internal.android.pixel.weatherhub.v1.ImageryService
```

| Method | Purpose |
|--------|---------|
| ListImages | Background weather imagery records for a location/condition |
| CreateImageRecords | Upload/create image records (write path) |

Auth class: API key allowed (same weatherhub key strategy).  
Not given a full live sample body in this reverse; host and method names confirmed from stubs `d.b.a.a.a.b.c.c` / `an`.
